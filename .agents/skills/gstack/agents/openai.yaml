interface:
  display_name: "astack"
  short_description: "Fast headless browser for QA testing and site dogfooding. Navigate pages, interact with elements, verify state, diff..."
  default_prompt: "Use astack for this task."
policy:
  allow_implicit_invocation: true
